image: node:22

stages:
  - build

before_script:
  - yarn --version
  - yarn install --frozen-lockfile

build_job:
  stage: build
  script:
    - yarn build
  artifacts:
    when: always
    paths:
      - dist
    expire_in: 1 hour
